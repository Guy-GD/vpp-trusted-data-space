## 关联 Issue

Closes #

## 模块与改动摘要

- 所属模块：
- 本次交付：
- 不包含的内容：

## 接口影响

- [ ] 无接口变更。
- [ ] 新增接口。
- [ ] 修改既有接口。

如涉及接口，请填写：

| 调用方 | HTTP 方法与路径 | 请求关键字段 | 响应关键字段 | 错误码 |
|---|---|---|---|---|
|  |  |  |  |  |

## 契约文档同步

- [ ] 不涉及接口路径、方法、字段、枚举、状态或错误码变更。
- [ ] 已更新 `docs/api/openapi.md`。
- [ ] 已更新 `docs/design/module-contracts.md`。
- [ ] 已更新 `docs/design/main-flow.md`（仅跨模块主流程或状态变化时）。
- [ ] 已更新 `docs/api/response-and-errors.md`（仅统一响应、错误码或追踪/幂等规则变化时）。

未更新但勾选不适用的原因：

## 验证证据

- 执行命令及结果：
- 正常路径请求或截图：
- 失败路径请求或截图：
- [ ] 后端：已验证 `GET /health` 和统一 `{ code, message, data, traceId }` 响应。
- [ ] 前端：已验证仅通过 `api-gateway` 访问后端能力。
- [ ] 涉及写操作：已验证 `Idempotency-Key` 重试行为。

## 联调与部署影响

- 网关联调：未开始 / 已完成 / 不适用。
- Mock 主流程：未开始 / 已通过 / 不适用。
- `docker-compose`：无影响 / 已更新并验证 / 当前尚不适用。
- 下游模块或数据依赖：

## 数据安全与隐私检查

- [ ] 未提交密钥、令牌、密码、`.env` 或真实凭据。
- [ ] 未记录或返回原始电表明文、未保护模型参数或敏感个人信息。
- [ ] 账本存证仅包含事件、摘要、URI 和审计元数据，不包含原始明文。

## 风险与回滚

- 已知限制或风险：
- 回滚方式：
